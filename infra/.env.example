# Cloudflare Tunnel token. Get this from:
#   one.dash.cloudflare.com → Networks → Tunnels → "palletballet" → connector install command.
# It's the long string after `--token ` in the docker run snippet Cloudflare gives you.
TUNNEL_TOKEN=
